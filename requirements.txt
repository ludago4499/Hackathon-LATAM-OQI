# Core
numpy>=1.24
pulp>=2.7            # classical MILP/LP baseline (bundles the CBC solver)
matplotlib>=3.7     # result plots

# Quantum (QAOA). PennyLane is the default in backend.py.
pennylane>=0.35

# Optional: Qiskit path (uncomment if you prefer Qiskit over PennyLane)
# qiskit>=1.0
# qiskit-aer>=0.14
